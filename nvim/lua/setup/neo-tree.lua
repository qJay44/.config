require('neo-tree').setup({
  window = {
    mappings = {
      ['o'] = {
        command = "open",
        nowait = true,
      },
      ["oc"] = "noop",
      ["od"] = "noop",
      ["og"] = "noop",
      ["om"] = "noop",
      ["on"] = "noop",
      ["os"] = "noop",
      ["ot"] = "noop",
    }
  }
})

