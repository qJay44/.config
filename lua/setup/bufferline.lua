local bufferline = require('bufferline')

bufferline.setup {
  highlights = {
    buffer_selected = {
        -- fg = '<colour-value-here>',
        -- bg = '<colour-value-here>',
        -- sp = '<colour-value-here>',
        bold = true,
        italic = false,
    },
  }
}

